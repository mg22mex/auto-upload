requests>=2.31.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
PyYAML>=6.0.0

# Phase 2+ (Facebook automation on fb-worker)
playwright>=1.44.0

# Phase 2 — Voice AI webhook
fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0
python-multipart>=0.0.9

# Phase 2 — PDF quote / spec sheets
reportlab>=4.0.0
